#include <stdio.h>
#include <stdlib.h>

int find_max_sum(int *numbers, int size)
{
	// Waiting to be implemented
    return -1;
}

#ifndef RunTests
int main()
{
    int v[] = { 5, 9, 7, 11 };
    printf("%d", find_max_sum(v, 4));
}
#endif
